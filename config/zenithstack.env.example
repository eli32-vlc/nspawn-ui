# ZenithStack Configuration Example

# Application Settings
ZENITH_DEBUG=false
ZENITH_HOST=0.0.0.0
ZENITH_PORT=8080

# Paths
ZENITH_DATA_DIR=/var/lib/zenithstack
ZENITH_LOG_DIR=/var/log/zenithstack
ZENITH_CONFIG_DIR=/etc/zenithstack

# Security
ZENITH_SECRET_KEY=change-me-to-a-random-string-in-production
ZENITH_ADMIN_USER=admin
ZENITH_ADMIN_PASSWORD_HASH=

# Network
ZENITH_IPV6_PREFIX=

# Database (optional, defaults to SQLite)
# ZENITH_DATABASE_URL=sqlite:///var/lib/zenithstack/zenithstack.db
# ZENITH_DATABASE_URL=postgresql://user:password@localhost/zenithstack
